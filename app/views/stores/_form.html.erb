<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :max_capacity %><br />
    <%= f.number_field :max_capacity %>
  </div>
  <div class="field">
    <%= f.label :current_capacity %><br />
    <%= f.number_field :current_capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
