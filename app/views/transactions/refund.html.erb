<h2>refund/exchange item</h2>
	
<%= form_for(@transaction) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	  <div class="field">
		<%= f.label :transaction, "Receipt Number"%><br/>
		<%= f.text_field :refund%>
	  </div>
	  
	  <div class="field">
	    <%= f.label :payment_type, "Exchange Type" %><br />
		<%= f.select :payment_type, [['refund', 'refund'],['exchange','exchange']] %>
	  </div>

	  <div class="actions">
	    <%= f.submit %>
	  </div>
<% end %>

<%= link_to 'back', current_transaction%>