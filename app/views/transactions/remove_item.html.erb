<h1>remove scanned item<h1>
	
	<%= form_for(@transaction) do |f| %>
	  <% if @transaction.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this product from being saved:</h2>

	      <ul>
	      <% @transaction.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <div class="field">
	    <%= f.label :product %><br />
	  	<%= f.collection_select :remove_item, get_array_product_list, :id, :name%>
	  </div>

	  <div class="actions">
	    <%= f.submit %>
	  </div>
	<% end %>

<%= link_to 'back', current_transaction%>